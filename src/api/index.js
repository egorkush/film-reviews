import films, * as syncFilms from './fake.api/films.api'
import * as syncGenres from './fake.api/genres.api'
import genresObject from './fake.api/genres.api'

const API = {
    films,
    genresObject,
    syncFilms,
    syncGenres
}

export default API